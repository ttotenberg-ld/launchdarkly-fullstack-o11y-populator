flask>=3.0.0
flask-cors>=4.0.0
launchdarkly-server-sdk>=9.12.0
launchdarkly-observability>=0.1.0
python-dotenv>=1.0.0
opentelemetry-api>=1.27.0
opentelemetry-sdk>=1.27.0
opentelemetry-instrumentation-flask>=0.48b0
opentelemetry-instrumentation-requests>=0.48b0
opentelemetry-instrumentation-sqlalchemy>=0.48b0
requests>=2.31.0
faker>=22.0.0
# 0.18.0 (2026-04-22) renamed `AICompletionConfig.tracker` → `.create_tracker()`
# callable and added at-most-once metric semantics. The chat-service code below
# is compatible with both APIs, but cap the upper bound here so a teammate's
# fresh rebuild doesn't surprise-pull a future breaking change.
launchdarkly-server-sdk-ai>=0.5.0,<0.19.0
ollama>=0.4.0
opentelemetry-instrumentation-ollama
sqlalchemy>=2.0.0
psycopg2-binary>=2.9.9
apscheduler>=3.10.0
