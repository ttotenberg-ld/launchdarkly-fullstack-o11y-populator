# Keep this pinned to match the base image tag in simulator/Dockerfile.
# Version skew between the pip package and the image crashes webkit at launch.
playwright==1.58.0
python-dotenv>=1.0.0
launchdarkly-server-sdk>=9.12.0
launchdarkly-observability>=0.1.0
faker>=22.0.0
